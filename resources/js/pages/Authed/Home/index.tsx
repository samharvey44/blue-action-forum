import AppContainer from 'app/components/layout/AppContainer';
import React from 'react';

const Home: React.FC = () => {
    return (
        <AppContainer>
            <p>Home</p>
        </AppContainer>
    );
};

export default Home;
